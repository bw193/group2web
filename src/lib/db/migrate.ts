// Migration is now handled by drizzle-kit.
// Run: npm run db:push   (push schema directly to Supabase)
// Run: npm run db:generate && npm run db:migrate  (for migration files)
console.log('Use "npm run db:push" to push schema to Supabase PostgreSQL.');
